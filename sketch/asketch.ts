// window.addEventListener("load", init);

// function init(): void {
//   const clickEvents = new ClickEvents();
  
//   let guessSpan: number = clickEvents.guessSpanRadios(); //make the user choose this with a range or dropdown
    
//   // this will be used by the CPU later when the actual game is initiated
//   let numberGenerator = new NumberGenerator();
//   let rng = numberGenerator.random(guessSpan);

//   // activate clickable buttons
//   clickEvents.toggleInstructions();
//   clickEvents.testButton(guessSpan)
// }
